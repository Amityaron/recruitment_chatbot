OPENAI_API_KEY=sk-...

# SQL Server (Windows Authentication). Defaults shown below match a local
# default-instance install — override only if your setup differs.
SQL_SERVER=localhost
SQL_DATABASE=Tech
